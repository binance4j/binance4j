---
title: Wallet endpoints
---

import ArtifactInstall from "./partials/_ArtifactInstall.mdx";
import RepoBadges from "./partials/_RepoBadges.mdx";
import Examples from "@site/src/components/v2/ApiExamples";

<RepoBadges artifact="wallet" />

## Installation

<ArtifactInstall repo="wallet" />

## Client instantiation

```java
WalletClient client = new WalletClient(key, secret);
```

## Features

### Get system status

<Examples payload="SystemStatus" method="getSystemStatus" items={[{}]} />

### Get all coins info

<Examples payload="CoinInformation>" method="getAllCoinsInfo" items={[{}]} />

### Get Spot account snapshot

<Examples
    payload="SpotAccountSnapshotResponse"
    method="getSpotAccountSnapshot"
    items={[{}, { label: "Framed", params: "new TimeFrame(limit)" }]}
/>

### Get Margin account snapshot

<Examples
    payload="MarginAccountSnapshotResponse"
    method="getMarginAccountSnapshot"
    items={[{}, { label: "Framed", params: "new TimeFrame(limit)" }]}
/>

### Get Futures account snapshot

<Examples
    payload="FuturesAccountSnapshotResponse"
    method="getFuturesAccountSnapshot"
    items={[{}, { label: "Framed", params: "new TimeFrame(limit)" }]}
/>

### Enable fast withdraw switch

<Examples payload="Void" method="enableFastWithdrawSwitch" items={[{}]} />

### Disable fast withdraw switch

<Examples payload="Void" method="disableFastWithdrawSwitch" items={[{}]} />

### Withdraw

<Examples
    payload="WithdrawResult"
    method="withdraw"
    items={[{ params: "new WithdrawParams(amount, asset, address)" }]}
/>

### Get deposit history

<Examples
    payload="List<DepositHistory>"
    method="getDepositHistory"
    items={[
        {},
        {
            label: "By asset",
            params: "new DepositHistoryParams(asset)",
        },
        {
            label: "By status",
            params: "new DepositHistoryParams(DepositStatus.SUCCESS)",
        },
        {
            label: "Both",
            params: "new DepositHistoryParams(DepositStatus.SUCCESS, asset)",
        },
        {
            label: "Framed",
            params: "new DepositHistoryParams(DepositStatus.SUCCESS, asset),new FramedPaging(page, limit)",
        },
    ]}
/>

### Get withdraw history

<Examples
    payload="List<WithdrawHistory>"
    method="getWithdrawHistory"
    items={[
        {},
        {
            label: "By status",
            params: "new DepositHistoryParams(WithdrawStatus.COMPLETED)",
        },
        {
            label: "By asset and status",
            params: "new DepositHistoryParams(asset, WithdrawStatus.COMPLETED)",
        },
        {
            label: "By withdraw order id",
            params: "new DepositHistoryParams(withdrawOrderId)",
        },
        {
            label: "Framed",
            params: "new DepositHistoryParams(asset, WithdrawStatus.COMPLETED), new FramedPaging(startTime, endTime, page)",
        },
    ]}
/>

### Get deposit address

<Examples
    payload="DepositAddress"
    method="getDepositAddress"
    items={[
        {},
        {
            label: "By coin",
            params: "new DepositAddressParams(coin)",
        },
        {
            label: "By coin and network",
            params: "new DepositAddressarams(coin, network)",
        },
    ]}
/>

### Get account status

<Examples payload="AccountStatus" method="getAccountstatus" items={[{}]} />

### Get API trading status

<Examples payload="ApiTradingStatus" method="getApiTradingStatus" items={[{}]} />

### Get dust log

<Examples
    payload="DustLog"
    method="getDustLog"
    items={[
        {},
        {
            label: "Framed",
            params: "new TimeFrame(startTime, endTime, limit)",
        },
    ]}
/>

### Do a dust transfer

<Examples
    payload="DustTransferResponse"
    method="dustTransfer"
    items={[
        {},
        {
            label: "Multiple symbols",
            params: "new DustTransferParams(symbols[])",
        },
    ]}
/>

### Get asset dividend record

<Examples
    payload="AssetDividendRecord"
    method="getAssetDividendRecord"
    items={[
        {},
        {
            label: "Specific symbol",
            params: "new AssetDividendRecordParams(symbol)",
        },
        {
            label: "Specific symbol framed",
            params: "new AssetDividendRecordParams(symbol), new TimeFrame(startTime, endTime, limit)",
        },
        {
            label: "Framed",
            params: "new TimeFrame(startTime, endTime, limit)",
        },
    ]}
/>

### Get asset detail

<Examples
    payload="Map<String, AssetDetail>"
    method="getAssetDetail"
    items={[
        {},
        {
            label: "Specific symbol",
            params: "new AssetDetailParams(symbol)",
        },
    ]}
/>

### Get trade fee

<Examples
    payload="List<TradeFee>"
    method="getTradeFee"
    items={[
        {},
        {
            label: "Specific symbol",
            params: "new TradeFeeParams(symbol)",
        },
    ]}
/>

### Make a universal transfer

<Examples
    payload="WalletTransferResponse"
    method="transfer"
    items={[
        {
            params: "new WalletTransferParams(asset, type, amount, fromSymbol, toSymbol)",
        },
    ]}
/>

### Get transfer history

<Examples
    payload="WalletTransferHistory"
    method="getTransferHistory"
    items={[
        {
            params: "new WalletTransferHistoryParams(type, fromSymbol, toSymbol)",
        },
    ]}
/>

### Get funding asset

Fetches the funding wallet asset balance

Currently supports querying the following business assets：Binance Pay, Binance Card, Binance Gift Card, Stock Token

<Examples
    payload="List<FundingAsset>"
    method="getFundingAsset"
    items={[
        {},
        {
            label: "Specific asset",
            params: "new FundingAssetParams(asset, needBtcValuation)",
        },
    ]}
/>

### Get API permissions

<Examples payload="ApiPermissions" method="getApiPermissions" items={[{}]} />
