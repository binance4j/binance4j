import Badge from "@site/src/components/badge/RequestBadges";
import data from "../data"

<Badge
    artifact="wallet"
    javadoc={`com/binance4j/wallet/${props.javadoc}`}
    version={data.version}
    weight={props.weight}
    signature={props.signature}
    path={props.path}
    label={props.label}
/>